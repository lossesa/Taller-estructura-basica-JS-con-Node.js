// 13) .map() + Multiplicación Condicional

// let numeros = [1,2,3,4,5,6];

// // Se usa map y el operador ternario dentro del map para colocar una condicion (duplicar solo los numeros pares) y los impare se dejan igual.
// const ejercicio = numeros.map(num => num % 2 == 0 ? num * num : num);
// console.log(ejercicio);
