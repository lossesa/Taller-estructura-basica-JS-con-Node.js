// 23) .find() — Buscar un número específico

// const numeros = [3,5,7,9,11];

// Find() devuelve el numero si lo encuentra, si no, devuelve undefined
// const encontrar = numeros.find((n) => n === 7);
// const mensaje = encontrar !== undefined ? "Encontrado": "No encontrado";
// console.log(mensaje);


// Ejemplo con some()
// const con_some = numeros.some((n) => n === 7) ? "Encontrado": "No encontrado";
// console.log(con_some);
